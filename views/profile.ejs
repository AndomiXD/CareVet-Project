<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>Home Page</title>
  </head>
  <body class="index-body">

  <div class="profile-container">
    <img class="profile-img" src="<%= user.image %>" alt="Profile Picture">
    <h2><%= user.firstName %> <%= user.lastName %></h2>
    <div class="profile-info">Username: <%= user.username %></div>
    <div class="profile-info">Address: <%= user.address %></div>
    <div class="profile-info">Phone: <%= user.phone %></div>
    <div class="profile-info">Role: <%= user.role %></div>
  </div>
  </body>
</html>
